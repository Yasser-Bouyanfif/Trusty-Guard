<% if user_signed_in? %>
<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-white rounded p-3 shadow">
        <h3>Création d'un devis</h3>
        <%= simple_form_for [@mission, @estimate] do |f| %>
          <%= f.input :comment, placeholder: 'Laissez un commentaire', :label => "Commentaire" %>
          <%= f.submit 'Envoyer mon devis', class: 'btn btn-secondary' %>
        <% end%>
      </div>
    </div>
  </div>
</div>
<% else %>
<%controller.redirect_to new_user_session_path %>
<% end %>
