<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.name %></strong>
    <i><%= message.created_at.strftime("%d %b %Y") %></i>
  </small>
  <p><%= message.content %></p>
</div>